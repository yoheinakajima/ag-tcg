- [cabt replay schema](cabt-replay-schema.md) — decode replay step/option type codes + prize/bench loss conditions honestly; numeric attackId only (no spread claims); dedupe Kaggle submission history by best-complete row.
- [tournament engine v0](pass36-tournament-engine.md) — reusable event-first resumable bounded-tick engine; run as repeated synchronous ticks (nohup dies at tool boundary); projections rebuild from ledger alone; single-run flock; gzip sidecar fallback.
- [EN_Card_Data csv quirks](card-data-csv-quirks.md) — "Pokémon Tool" contains "pok" (classify trainer markers before pokemon); Dragon energy_type is glyph 竜 not {N} (honest non-match, no fake color fix).
- [pass gating + report honesty](pass-gating-honesty.md) — held-probe retention must AND the no-collapse gate into a persisted conjunction; report root-safety must be filecmp-derived (yes/no/unverified), never hardcoded yes/PASS.
- [shared-artifact stale tests](pass-shared-artifact-stale-tests.md) — single shared canvas/site/report/queue/meta_pool overwritten each pass; older strict snapshot tests go red structurally (not a regression); write your pass's tests forward-compatibly.
- [cabt native engine hangs](cabt-native-engine-hangs.md) — every cabt game in a hard-timeout subprocess (in-proc SIGALRM can't interrupt native C); for big tournaments use an import-once batch worker streaming flushed persistent JSONL + start/result markers.
- [scheduled-deployment ops](pass38-scheduled-deployment-ops.md) — manifest-sha HARD in prod / WARN in local (disposable dir); "published ≠ observed running" (verify by tick signature + logs); pin runbook verbatim to .replit; script-as-module test gotcha (sys.modules before exec).
- [typed-layer live inertness](typed-layer-live-inertness.md) — fixture non-inertness vs live firing (~1%) differ; value is safety not strength; at small n prefer Fisher-exact (H2H vs pooled self-mirror) over CI-non-overlap; never extend parent-beating to public refs.
- [kaggle-env co_argcount](kaggle-env-co-argcount.md) — kaggle_environments slices agent args by callable co_argcount; bare def f(*args) gets obs=None and silently no-ops — pass an INSTANCE with bound __call__(self,obs,...).
- [idempotent event marker](idempotent-event-marker.md) — re-runnable delete-then-reappend emitters must strip by a UNIQUE sub-step marker tag, never the shared pass tag, else they delete sibling sub-steps' events.
- [reference benchmark lane](pass40-reference-benchmark-lane.md) — validate tar members before extracting untrusted external tarballs; refs are benchmark-only in a separate no_upload ledger (zero-leakage); bounded worklists emit both seats/pair before expanding (else truncated cap → all-seat0).
- [lifecycle auto-quarantine evidence](lifecycle-auto-quarantine-evidence.md) — auto-applied demotions need COMPLETE hard-failure evidence (invalid==games); draws are non-decisive but NOT failures, so decisive==0 alone must never trigger auto-quarantine.
- [scheduled-deployment worker](pass37-scheduled-deployment.md) — wrap unchanged engine as bounded Scheduled Deployment (not root workflow/server); disposable working dir + fail-closed persistent storage; reconcile by event_id, GameFinished idempotent on sha only, scheduled/started on curated identity allowlist; base=None push is drift→merge; TTL/interval/timeout invariant.
- [ledger↔manifest lockstep](tournament-ledger-manifest-lockstep.md) — any offline op that appends to the tournament ledger must locally refresh storage_manifest.json (sync.build_manifest, no_upload) or the pass38 manifest_event_count_matches health check goes hard-red.
